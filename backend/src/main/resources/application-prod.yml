spring:
  config:
    import:
      - application-static.yml  # 静态配置
      - application-hikari.yml  # HikariCP连接池配置
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:15432/difyapp  # 生产环境数据库地址
    username: user_fcPsSr
    password: password_Xw4iyM
  jpa:
    hibernate:
      ddl-auto: validate  # 生产环境使用validate，确保数据库结构正确
    show-sql: false  # 生产环境关闭SQL打印
  data:
    redis:
      host: localhost
      port: 6379
      password: redis_xdYB4S  # 生产环境Redis密码

jwt:
  secret: dify-app-secret-key-2024-this-is-a-very-long-secret-key-for-security  # 生产环境必须修改为更安全的密钥

minio:
  endpoint: http://localhost:9000
  access-key: rustfsadmin
  secret-key: rustfsadmin

observability:
  enabled: true
  thread-pool:
    core-size: 8  # 生产环境使用较大的线程池
    max-size: 16
    queue-capacity: 1000
