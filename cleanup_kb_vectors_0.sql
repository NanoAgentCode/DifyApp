-- ============================================
-- 清理 kb_vectors_0 表的 SQL 脚本
-- 用途：解决向量维度不匹配问题（期望: 4096, 实际: 4092）
-- ============================================

-- 方案1：删除表及其所有数据（推荐）
-- 执行后，系统会在下次上传文档时自动创建正确维度的表
DROP TABLE IF EXISTS kb_vectors_0 CASCADE;

-- 方案2：只删除数据，保留表结构（不推荐，因为表结构维度不匹配）
-- 如果选择此方案，还需要手动修改表结构
-- TRUNCATE TABLE kb_vectors_0;
-- ALTER TABLE kb_vectors_0 ALTER COLUMN embedding TYPE vector(4096);

-- ============================================
-- 执行说明：
-- 1. 推荐使用方案1（DROP TABLE），删除后系统会自动创建正确维度的表
-- 2. 执行前请确保已备份重要数据（如果有）
-- 3. 执行后需要重新上传文档，系统会自动创建维度为 4096 的表
-- ============================================

